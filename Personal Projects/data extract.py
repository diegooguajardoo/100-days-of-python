#excerice 6,14 from the book. Extracts data from str and converts it to float
str = 'X-DSPAM:Confidence:0.8475'
pos = str.find(":")
#floatdata = float(str[pos+1:])
print(pos)

#if type(floatdata) == float:
#	print("Success!")
#else:
#	print("Fail!")